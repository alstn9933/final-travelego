<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="member">
 <select id="loginMember" parameterType="member" resultType="member">
 	select 
 	member_id as memberId,
 	member_pw as memberPw,
 	member_nickname as memberNickname,
 	member_name as memberName,
 	age,
 	gender,
 	phone,
 	email,
 	member_level as memberLevel
 	from member where member_id=#{memberId} and member_pw=#{memberPw}
 </select>
 <insert id="memberjoin" parameterType="member">
 	insert into member values(#{memberId},#{memberPw},#{memberNickname},#{memberName},#{age},#{gender},#{phone},#{email},1)
 </insert>
  <insert id="companymemberjoin" parameterType="member">
 	insert into member values(#{memberId},#{memberPw},#{memberNickname},#{memberName},#{age},#{gender},#{phone},#{email},2)
 </insert>
</mapper>
